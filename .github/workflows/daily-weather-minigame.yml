name: Daily weather minigame

on:
  schedule:
    - cron: "0 13-17 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-weather-minigame
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Generate weather game
        run: node scripts/generate-daily-weather-game.mjs "$(TZ=America/Los_Angeles date +%F)"

      - name: Check today's weather game exists
        run: node scripts/test-daily-weather-current.mjs "$(TZ=America/Los_Angeles date +%F)"

      - name: Check old daily game is removed
        run: npm run test:weather-replacement

      - name: Commit generated game
        run: |
          if git diff --quiet -- minigames/index.html; then
            echo "Weather minigame already up to date."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add minigames/index.html
          git commit -m "Generate daily weather minigame"
          git push origin HEAD:main
